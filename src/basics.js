// Basic TypeScript example compiled to JavaScript
function greet(name) {
    console.log("Hello, " + name + "!");
}

greet("World");

// Let's add some type definitions
var age = 25;
var names = ["John", "Jane", "Alice"];

// Simple interface equivalent
var person = {
    name: "John Doe",
    age: 30
};

console.log(person);
